<div class="row col-12">
    {% if product.picture is not null or product.thumbnail is not null %}
    <div class="col-12 col-lg-4">
        {% if product.picture is not null %}
        <img src="{{ asset(product.picture) }}" alt="Image de {{ product.title}}" style="max-width: 200px;" class="mx-auto d-block" id="product_imageUrl">
        {% endif %}
        {% if product.thumbnail is not null %}
        <img src="{{ asset(product.thumbnail) }}" alt="Thumbnail de {{ product.title}}" style="max-width: 200px;" class="mx-auto d-block" id="product_thumbnailUrl">
        {% endif %}
    </div>
    <div class="col-12 col-lg-8 border-left">
    {% else %}
        <div class="col-12 border-left">
    {% endif %}
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
            {{ form_end(form) }}
        </div>
    </div>

    <script type="text/javascript">
        document.querySelectorAll('[type="file"]')
            .forEach(function(input) {
                input.addEventListener("change", function(e) {
                    e.currentTarget.closest('.custom-file').querySelector('label').textContent = e.currentTarget.value;
                });
            });
    </script>