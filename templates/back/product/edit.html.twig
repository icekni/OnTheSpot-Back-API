{% extends 'base.html.twig' %}

{% block title %}Edit Product{% endblock %}

{% block body %}
<div class="row">
    {% set referer = app.request.server.get('HTTP_REFERER')|default('/') %}
    <h1 class='mx-auto'>{{ product.name }} <a href="{{ path('product_show', {'id': product.id}) }}" class="h6">Retour</a></h1>

</div>

<div class="row mt-3">
    {{ include('back/product/_form.html.twig') }}
</div>
{% endblock %}